<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.tobebetter.dao.ProposeDao">
    <!--设置domain类和数据库中表的字段一一对应，注意数据库字段和domain类中的字段名称不致，此处一定要！-->
    <resultMap id="BaseResultMap" type="xyz.tobebetter.entity.Propose">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="connect" property="connect" jdbcType="CHAR" />
        <result column="message" property="message" jdbcType="CHAR" />
        <result column="email" property="email" jdbcType="CHAR" />
        <result column="createDate" property="createDate" jdbcType="INTEGER" />
        <result column="updateDate" property="updateDate" jdbcType="INTEGER" />
    </resultMap>


    <!-- 查询单条记录 -->

    <select id="findAll" resultType="java.lang.Long">
        SELECT * FROM PROPOSE
    </select>
    <!--<select id="getCount" resultType="java.lang.Long">-->
        <!--SELECT count(*) FROM PROPOSE-->
    <!--</select>-->
    <!--<select id="getByPage" parameterType="com.leqienglish.model.Page"  resultMap="BaseResultMap">-->
        <!--SELECT * FROM PROPOSE limit #{page},#{pageSize}-->
    <!--</select>-->
    <delete id="delete" parameterType="java.lang.String"  >
        DELETE from PROPOSE where ID = #{id}
    </delete>
    <insert id="create" useGeneratedKeys="true" keyProperty="id" parameterType="xyz.tobebetter.entity.Propose">
        INSERT INTO PROPOSE (ID ,CONNECT,MESSAGE,CREATEDATE,UPDATEDATE,STATUS) VALUES (#{id},#{connect},#{message},#{createDate},#{updateDate},#{status})
    </insert>
    <!--<update id="update" parameterType="com.leqigame.entity.User">-->
        <!--UPDATE USER SET NAME =#{name} ,EMAIL=#{email},PASSWORD=#{password},CREATEDATE=#{createDate},UPDATEDATE=#{updateDate} where ID=#{id}-->
    <!--</update>-->

</mapper>